import { Button } from "@mantine/core";

export default async function PlaygroundPage() {
  return (
    <div className="p-5 container max-w-lg">
      <Button variant="filled">Button</Button>
    </div>
  );
}
